---
title: Novus Ordo Conservatorum
---

%section
  %h2 Character creation
  %table
    - (1..2).each do |i|
      %tbody
        %tr
          = render '/logs/_session.haml', log: @items["/logs/noc/%02d.txt" % i]
%section
  %h2 Chapter 1
  %table
    - (3..10).each do |i|
      %tbody
        %tr
          = render '/logs/_session.haml', log: @items["/logs/noc/%02d.txt" % i]
%section
  %h2 Chapter 2
  %table
    - (11..20).each do |i|
      %tbody
        %tr
          = render '/logs/_session.haml', log: @items["/logs/noc/%02d.txt" % i]
%section
  %h2 Chapter 3
  %table
    %tbody
      %tr
        = render '/logs/_session.haml', log: @items['/logs/noc/21.txt']
